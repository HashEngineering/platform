# Pins the native library for the UNIFIED flavor (full SDK + key-wallet +
# platform-wallet + shielded). Read by org.dash.sdk.ffi.NativeLibrary from the
# classpath. The `dash.sdk.native.lib` system property still overrides this at runtime.
dash.sdk.native.lib=rs_unified_sdk_ffi
